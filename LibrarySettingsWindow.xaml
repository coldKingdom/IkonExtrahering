<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Biblioteksinställningar"
        Width="640" Height="450" MinWidth="560" MinHeight="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#F5F6F8"
        FontFamily="Segoe UI" FontSize="13">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2864DC"/>
        <SolidColorBrush x:Key="TextBrush" Color="#172033"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#68758B"/>
        <SolidColorBrush x:Key="StrokeBrush" Color="#DCE2EA"/>
        <Style x:Key="ModernButton" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F5F7FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F6F8">
      <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Biblioteksplats" FontSize="21" FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="Välj var ikonbiblioteket ska lagras och kom ihåg valet på den här datorn."
                       Margin="0,5,0,0" Foreground="{StaticResource MutedTextBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,22,0,20" Padding="18" Background="White"
                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Aktiv plats" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                    <Border Grid.Column="1" Background="#EAF1FF" CornerRadius="9" Padding="8,3">
                        <TextBlock x:Name="PathTypeText" Text="Lokal standardplats"
                                   FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
                    </Border>
                </Grid>
                <Border Grid.Row="1" Margin="0,10,0,0" Padding="12,10" Background="#F8FAFC"
                        BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" CornerRadius="8">
                    <TextBlock x:Name="LibraryPathText" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/>
                </Border>
                <StackPanel Grid.Row="2" Margin="0,12,0,0" Orientation="Horizontal">
                    <Button x:Name="ChooseFolderButton" Style="{StaticResource ModernButton}"
                            Content="Välj mapp…" Margin="0,0,9,0"/>
                    <Button x:Name="UseLocalButton" Style="{StaticResource ModernButton}"
                            Content="Använd lokal standardplats"/>
                </StackPanel>
                <TextBlock Grid.Row="3" Margin="0,14,0,0" TextWrapping="Wrap"
                           Foreground="{StaticResource MutedTextBrush}" FontSize="12"
                           Text="Befintliga ikoner flyttas inte automatiskt. Välj den tidigare platsen igen för att återgå till det biblioteket."/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Style="{StaticResource ModernButton}"
                    Content="Avbryt" Width="100" Margin="0,0,9,0" IsCancel="True"/>
            <Button x:Name="SaveButton" Style="{StaticResource PrimaryButton}"
                    Content="Spara plats" Width="125" IsDefault="True"/>
        </StackPanel>
      </Grid>
    </Grid>
</Window>
